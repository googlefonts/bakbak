table head { FontRevision 0.200; } head;
include(../../features/classes.fea);
include(../../features/tables.fea);
include(../../features/languagesystems.fea);
include(../../features/GSUB_lookups.fea);
include(../../features/GSUB.fea);
feature kern { include(kern.fea); } kern;
include(markclasses.fea);
feature abvm { include(abvm.fea); } abvm;
feature blwm { include(blwm.fea); } blwm;
